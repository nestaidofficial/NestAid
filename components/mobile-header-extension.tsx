"use client"

import React from "react"


interface MobileHeaderExtensionProps {
  isAdminPage: boolean
}

export function MobileHeaderExtension({ isAdminPage }: MobileHeaderExtensionProps) {
  // Hide mobile header extension (phone number removed as requested)
  return null
} 